;; Test-superset mono coordinate. Same git sha as ../mono, mono-test-lib
;; root (adds test-system + testcontainers). Projects reference this under
;; :test as ext/mono so it replaces the runtime root by symbol.
;; Bump the tag/sha to match ../mono.
{:deps {com.repldriven/mono
        {:git/url "https://github.com/repldriven/mono.git"
         :git/tag "v0.0.13"
         :git/sha "c4d76c643696baba4e2f04db984c9843fa00c7ee"
         :deps/root "projects/mono-test-lib"}}}
