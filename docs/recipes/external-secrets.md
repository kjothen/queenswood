# External secrets

<!-- tessl-plugin: deployment -->

## Problem

A workload needs a credential that no automation can produce: a key a
provider issued once, a secret minted in somebody's console, a value
generated by hand and never again. It has to reach a pod, and every
place you might put it on the way is worse than where it is going.

Git is out, private repository or not — history is permanent, read
access is coarse, and clones proliferate. A cluster Secret is where it
ends up but not where it lives: etcd does not survive a rebuilt cluster,
nothing audits a read, and placing one by hand means somebody holds
write access to the cluster.

## Solution

One entry per credential in Secret Manager, and three parties who each
hold exactly one part of it.

### Three parties, one each

**A composite declares the container.** Whichever composite owns the
thing that needs the credential composes a `Secret` — the entry, with
no version in it. What exists is therefore declared, and what it holds
never touches git. The entry is named as
[cloud-naming](cloud-naming.md) names everything else:
`sec-<code>-c-<what>` for the plane's, and
`sec-<code>-<env>-<label>-<what>` for an instance's.

**A person writes the version.** Once, from the `secretsAdmin`
capability. This does not become automated by anything: the value came
from outside, and composing a container is the most a composite can do.

**An operator reads it on the destination cluster.** external-secrets,
authenticated by Workload Identity, with a `ClusterSecretStore` carrying
no auth block — the controller's pod already runs as the annotated
service account, so the provider takes the identity it has. An
`ExternalSecret` then materialises the value into a Secret in the
namespace that consumes it.

The read happens on the cluster that needs the value, which is the
property worth protecting: Argo never holds it, no pipeline sees it,
and rebuilding the cluster re-reads rather than re-uploads.

Installing that operator through Argo needs `ServerSideApply=true` —
its `SecretStore` and `ClusterSecretStore` CRDs exceed 256KB. See
[argocd](argocd.md) for what happens when it is missing, and for the
waves that put the store ahead of what reads from it.

### Putting a version in

```
just gcp-secret-version <secret> [project] [file]
```

It finds the project from the entry's name where the name carries one,
refuses to create a container the composite has not made, and keeps the
value off the command line. With no `file` it prompts and does not echo,
so the value reaches Secret Manager without being written down.

A typed or piped value loses its trailing newline, deliberately. Secret
Manager stores the bytes it is given, and a consumer that reads the
value whole sends that newline as part of the credential — which fails
as the far end rejecting a wrong secret, and mentions nothing about a
newline. A `file` goes in byte for byte, because a key is not text to be
tidied.

### Where the value must not go on the way

**Not a file you delete afterwards.** `rm` is not deletion on a
copy-on-write filesystem: the blocks survive, and so do local snapshots,
backups, the search index, and whatever swap file the editor left. A
file you cannot prove is gone is worse than one you know is there.

**Not `pass`, or any other local store.** That is a second durable copy
of the credential, on one machine, under one key, with no audit trail —
which is what
[ADR-0022](../adr/0022-cloud-foundation-and-environment-lifecycle.md)
named as the previous generation's failure. Two authorities also means
two places to rotate, and one of them will be forgotten.

Paste it into the prompt instead, and then overwrite the clipboard.
A clipboard that syncs between devices is the exposure that remains,
and it is the one you can close in a second.

### Rotatable decides how much care

The question that settles how a credential is handled is whether losing
it costs anything.

**Rotatable** — a Google client secret, a GitHub App key. Regenerating
is a click, so there is nothing to back up, and a copy is pure added
exposure buying no recovery. Keep none, anywhere.

**Not rotatable** — an encryption key that existing backups were
written with. Losing it loses what it protects, so its entry withholds
`Delete` from `managementPolicies` and *is* the durable copy. That is a
reason to protect the entry, not a reason to keep a second one on a
laptop.

### The ones that exist

**`sec-<code>-c-github-app`**, on the management plane. The App id, the
installation id and the private key as one JSON object, so the
identifiers travel with the key rather than through a second channel.
It arrives through `gcp-github-app-secret`, which composes that object
and pipes it in. File-shaped, because GitHub hands you a download rather
than something to type. Rotatable, and an App may hold two keys at once
— add the new one, update the entry, then delete the old. See
[argocd-github](argocd-github.md).

**`sec-<code>-<env>-<label>-google-oauth`**, on an instance. A single
string: the OAuth client's secret. The `ExternalSecret` materialises it
into the file vault Keycloak mounts, under the filename that vault
expects — `<realm>_<key>` — so the realm's committed
`${vault.google-client-secret}` resolves. Rotatable. See
[google-sign-in](google-sign-in.md).

**The FoundationDB backup encryption key**, on an instance, and not yet
composed. It is the not-rotatable case: an existing backup is only
readable with the key it was written under. Today it lives in `pass` in
the previous generation, which is precisely what this replaces.

### Where it goes wrong quietly

**A container with no version.** The composite composes the entry and
the operator syncs it, both successfully, and what reaches the cluster
is empty. The failure then surfaces wherever the value was used — a
repository reported unreachable, a client rejected — rather than as a
credential that was never written.

**A release name that is not the one the binding names.** Workload
Identity matches `<project>.svc.id.goog[<namespace>/<serviceaccount>]`
literally. Let Argo derive the release name from the Application's name
and the chart produces a service account nothing is bound to, and the
controller fails to authenticate against a binding that looks correct.

**An hour of nothing happening.** `refreshInterval` is an hour, so a
version added after the `ExternalSecret` synced does not appear until
the next refresh. Delete the controller's pod to hurry it — and note
that a consumer reading its value at startup, as Keycloak's file vault
does, needs a restart after that.

## Rules

**MUST:**

- Compose the container and write the version separately: a composite
  declares an entry with no version, and a person adds one.
- Read on the destination cluster, through an operator authenticated by
  Workload Identity, so neither git nor Argo ever holds the value.
- Give the `ClusterSecretStore` no auth block. The controller's pod
  carries the identity.
- Pin the release name and the service account name the Workload
  Identity binding spells, rather than letting either be derived.
- Withhold `Delete` from the entry's `managementPolicies` where the
  value cannot be regenerated.
- Put a version in with `just gcp-secret-version`, and let it strip the
  trailing newline from anything typed or piped.

**MUST NOT:**

- Commit a credential, private repository or not.
- Keep a second durable copy of a credential that can be regenerated —
  not in a local store, and not in a file deleted afterwards.
- Create a missing container by hand. Its absence means the composite
  has not reconciled, and one made here is one nothing declares.

**SHOULD:**

- Overwrite the clipboard after pasting a value into the prompt.
- Delete the controller's pod rather than waiting out the refresh
  interval, and restart whatever reads the value at startup.

**MAY:**

- Pass a `file` where the value is not something to type — a key or a
  certificate, which arrives as a download rather than as a string.
- Hold several fields in one entry as JSON, where they identify each
  other and would otherwise travel separately.

## References

- [argocd-github](argocd-github.md) — the App whose key is the first of
  these, and what Argo does with it.
- [google-sign-in](google-sign-in.md) — the console act that produces
  the second, and the vault filename it has to land under.
- [argocd](argocd.md) — server-side apply for the operator's CRDs, and
  the waves that order a store ahead of its reader.
- [gcp-iam](gcp-iam.md) — both halves of a Workload Identity binding,
  and why one half alone reports nothing.
- [cloud-naming](cloud-naming.md) — what an entry is called, and what
  the code, environment and label in its name are.
- [ADR-0022](../adr/0022-cloud-foundation-and-environment-lifecycle.md)
  — Secret Manager in the management project, and the one-machine store
  it replaces.
