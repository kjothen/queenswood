  # The public zone, rendered by `just queenswood-dns-manifest-snippet`
  # from what the domain answers today. Every rrdata is quoted twice:
  # the inner pair is part of a TXT value and the outer pair is YAML's,
  # and one set answers as a different value.
  dns:
    domain: "${DOMAIN}"
    records:
${RECORDS}
