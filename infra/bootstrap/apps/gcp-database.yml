apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: gcp-database
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "3"
    description: CloudSQL Postgres for Keycloak
spec:
  project: default
  source:
    repoURL: https://github.com/repldriven/queenswood
    targetRevision: main
    path: infra/gcp/database
  destination:
    server: https://kubernetes.default.svc
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
