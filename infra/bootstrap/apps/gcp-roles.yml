apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: gcp-roles
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "1"
    description: Project IAM bindings for the Crossplane SA
spec:
  project: default
  source:
    repoURL: https://github.com/repldriven/queenswood
    targetRevision: main
    path: infra/gcp/roles
  destination:
    server: https://kubernetes.default.svc
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
