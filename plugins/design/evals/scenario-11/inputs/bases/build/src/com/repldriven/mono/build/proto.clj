(ns com.repldriven.mono.build.proto)

(defn gen-proto
  "Shared protoc + protojure generation logic. opts carries the
  brick's paths."
  [opts]
  ;; runs protoc, writes generated sources into the brick's gen/
  nil)
