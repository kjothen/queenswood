source_up_if_exists
if has nix; then
  use flake .
else
  echo "nix not found — run scripts/check-setup.sh for install instructions"
fi

echo "Prepping libraries..."
clj -X:deps prep :aliases '[:dev]'

echo "Exporting Bank Admin API Key...for development only"
export ADMIN_API_KEY="ak_test_jMEFXuaZKrVB26jTLHV6-AZOzhIYq44NBEkwbRwpogg"
export VITE_ADMIN_API_KEY="$ADMIN_API_KEY"
