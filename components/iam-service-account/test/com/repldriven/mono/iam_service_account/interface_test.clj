(ns com.repldriven.mono.iam-service-account.interface-test
  (:require [clojure.test :as test :refer :all]
            [com.repldriven.mono.iam-service-account.interface :as SUT]))

(deftest dummy-test (is (= 1 1)))
