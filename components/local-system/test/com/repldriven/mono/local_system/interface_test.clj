(ns com.repldriven.mono.local-system.interface-test
  (:require [clojure.test :as test :refer :all]
            [com.repldriven.mono.local-system.interface :as local-system]))

(deftest dummy-test
  (is (= 1 1)))
