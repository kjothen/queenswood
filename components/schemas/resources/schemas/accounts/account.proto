syntax = "proto2";

package com.repldriven.mono.schemas.accounts;

import "record_metadata_options.proto";

option java_outer_classname = "AccountProto";

message ScanAddress {
  required string sort_code = 1;
  required string account_number = 2;
}

message PaymentAddress {
  required string scheme = 1;
  oneof identifier {
    ScanAddress scan = 2;
    string value = 3;
  }
}

message Account {
  option (com.apple.foundationdb.record.record).usage = RECORD;

  required string account_id = 1 [(com.apple.foundationdb.record.field).primary_key = true];
  optional string customer_id = 2;
  optional string name = 3;
  optional string currency = 4;
  optional string status = 5;
  optional int64 created_at_ms = 6;
  optional int64 updated_at_ms = 7;
  repeated PaymentAddress payment_addresses = 10;
}
