;;;----------------------------------------------------------------------------------
;;; Generated by protoc-gen-clojure.  DO NOT EDIT
;;;
;;; Message Implementation of package com.repldriven.mono.schemas.organizations
;;;----------------------------------------------------------------------------------
(ns com.repldriven.mono.schemas.organizations
  (:require
    [protojure.protobuf.protocol :as pb]
    [protojure.protobuf.serdes.core :as serdes.core]
    [protojure.protobuf.serdes.complex :as serdes.complex]
    [protojure.protobuf.serdes.utils :refer [tag-map]]
    [protojure.protobuf.serdes.stream :as serdes.stream]
    [clojure.set :as set]
    [clojure.spec.alpha :as s]))

;;----------------------------------------------------------------------------------
;;----------------------------------------------------------------------------------
;; Forward declarations
;;----------------------------------------------------------------------------------
;;----------------------------------------------------------------------------------

(declare cis->OrganizationChangelog)
(declare ecis->OrganizationChangelog)
(declare new-OrganizationChangelog)
(declare cis->Organization)
(declare ecis->Organization)
(declare new-Organization)


;;----------------------------------------------------------------------------------
;;----------------------------------------------------------------------------------
;; Message Implementations
;;----------------------------------------------------------------------------------
;;----------------------------------------------------------------------------------

;-----------------------------------------------------------------------------
; OrganizationChangelog
;-----------------------------------------------------------------------------
(defrecord OrganizationChangelog-record [organization-id status-before
                                         status-after]
  pb/Writer
    (serialize [this os]
      (serdes.core/write-String 1 {:optimize true} (:organization-id this) os)
      (serdes.core/write-String 2 {:optimize true} (:status-before this) os)
      (serdes.core/write-String 3 {:optimize true} (:status-after this) os))
  pb/TypeReflection
    (gettype [this]
      "com.repldriven.mono.schemas.organizations.OrganizationChangelog"))

(s/def
  :com.repldriven.mono.schemas.organizations.OrganizationChangelog/organization-id
  string?)
(s/def
  :com.repldriven.mono.schemas.organizations.OrganizationChangelog/status-before
  string?)
(s/def
  :com.repldriven.mono.schemas.organizations.OrganizationChangelog/status-after
  string?)
(s/def ::OrganizationChangelog-spec
  (s/keys
   :opt-un
   [:com.repldriven.mono.schemas.organizations.OrganizationChangelog/organization-id
    :com.repldriven.mono.schemas.organizations.OrganizationChangelog/status-before
    :com.repldriven.mono.schemas.organizations.OrganizationChangelog/status-after]))
(def OrganizationChangelog-defaults
  {:organization-id "" :status-before "" :status-after ""})

(defn cis->OrganizationChangelog
  "CodedInputStream to OrganizationChangelog"
  [is]
  (->> (tag-map OrganizationChangelog-defaults
                (fn [tag index]
                  (case index
                    1 [:organization-id (serdes.core/cis->String is)]
                    2 [:status-before (serdes.core/cis->String is)]
                    3 [:status-after (serdes.core/cis->String is)]

                    [index (serdes.core/cis->undefined tag is)]))
                is)
       (map->OrganizationChangelog-record)))

(defn ecis->OrganizationChangelog
  "Embedded CodedInputStream to OrganizationChangelog"
  [is]
  (serdes.core/cis->embedded cis->OrganizationChangelog is))

(defn new-OrganizationChangelog
  "Creates a new instance from a map, similar to map->OrganizationChangelog except that
  it properly accounts for nested messages, when applicable.
  "
  [init]
  {:pre [(if (s/valid? ::OrganizationChangelog-spec init)
           true
           (throw (ex-info "Invalid input"
                           (s/explain-data ::OrganizationChangelog-spec
                                           init))))]}
  (-> (merge OrganizationChangelog-defaults init)
      (map->OrganizationChangelog-record)))

(defn pb->OrganizationChangelog
  "Protobuf to OrganizationChangelog"
  [input]
  (cis->OrganizationChangelog (serdes.stream/new-cis input)))

(def ^:protojure.protobuf.any/record OrganizationChangelog-meta
  {:type "com.repldriven.mono.schemas.organizations.OrganizationChangelog"
   :decoder pb->OrganizationChangelog})

;-----------------------------------------------------------------------------
; Organization
;-----------------------------------------------------------------------------
(defrecord Organization-record [organization-id name status created-at
                                updated-at]
  pb/Writer
    (serialize [this os]
      (serdes.core/write-String 1 {:optimize true} (:organization-id this) os)
      (serdes.core/write-String 2 {:optimize true} (:name this) os)
      (serdes.core/write-String 3 {:optimize true} (:status this) os)
      (serdes.core/write-Int64 4 {:optimize true} (:created-at this) os)
      (serdes.core/write-Int64 5 {:optimize true} (:updated-at this) os))
  pb/TypeReflection
    (gettype [this] "com.repldriven.mono.schemas.organizations.Organization"))

(s/def :com.repldriven.mono.schemas.organizations.Organization/organization-id
  string?)
(s/def :com.repldriven.mono.schemas.organizations.Organization/name string?)
(s/def :com.repldriven.mono.schemas.organizations.Organization/status string?)
(s/def :com.repldriven.mono.schemas.organizations.Organization/created-at int?)
(s/def :com.repldriven.mono.schemas.organizations.Organization/updated-at int?)
(s/def ::Organization-spec
  (s/keys
   :opt-un
   [:com.repldriven.mono.schemas.organizations.Organization/organization-id
    :com.repldriven.mono.schemas.organizations.Organization/name
    :com.repldriven.mono.schemas.organizations.Organization/status
    :com.repldriven.mono.schemas.organizations.Organization/created-at
    :com.repldriven.mono.schemas.organizations.Organization/updated-at]))
(def Organization-defaults
  {:organization-id "" :name "" :status "" :created-at 0 :updated-at 0})

(defn cis->Organization
  "CodedInputStream to Organization"
  [is]
  (->> (tag-map Organization-defaults
                (fn [tag index]
                  (case index
                    1 [:organization-id (serdes.core/cis->String is)]
                    2 [:name (serdes.core/cis->String is)]
                    3 [:status (serdes.core/cis->String is)]
                    4 [:created-at (serdes.core/cis->Int64 is)]
                    5 [:updated-at (serdes.core/cis->Int64 is)]

                    [index (serdes.core/cis->undefined tag is)]))
                is)
       (map->Organization-record)))

(defn ecis->Organization
  "Embedded CodedInputStream to Organization"
  [is]
  (serdes.core/cis->embedded cis->Organization is))

(defn new-Organization
  "Creates a new instance from a map, similar to map->Organization except that
  it properly accounts for nested messages, when applicable.
  "
  [init]
  {:pre [(if (s/valid? ::Organization-spec init)
           true
           (throw (ex-info "Invalid input"
                           (s/explain-data ::Organization-spec init))))]}
  (-> (merge Organization-defaults init)
      (map->Organization-record)))

(defn pb->Organization
  "Protobuf to Organization"
  [input]
  (cis->Organization (serdes.stream/new-cis input)))

(def ^:protojure.protobuf.any/record Organization-meta
  {:type "com.repldriven.mono.schemas.organizations.Organization"
   :decoder pb->Organization})

