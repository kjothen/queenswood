;;;----------------------------------------------------------------------------------
;;; Generated by protoc-gen-clojure.  DO NOT EDIT
;;;
;;; Message Implementation of package com.repldriven.mono.schemas.cash_accounts
;;;----------------------------------------------------------------------------------
(ns com.repldriven.mono.schemas.cash_accounts
  (:require
    [protojure.protobuf.protocol :as pb]
    [protojure.protobuf.serdes.core :as serdes.core]
    [protojure.protobuf.serdes.complex :as serdes.complex]
    [protojure.protobuf.serdes.utils :refer [tag-map]]
    [protojure.protobuf.serdes.stream :as serdes.stream]
    [com.repldriven.mono.schemas.cash_accounts :as
     com.repldriven.mono.schemas.cash_accounts]
    [clojure.set :as set]
    [clojure.spec.alpha :as s]))

;;----------------------------------------------------------------------------------
;;----------------------------------------------------------------------------------
;; Forward declarations
;;----------------------------------------------------------------------------------
;;----------------------------------------------------------------------------------

(declare cis->ScanAddress)
(declare ecis->ScanAddress)
(declare new-ScanAddress)
(declare cis->PaymentAddress)
(declare ecis->PaymentAddress)
(declare new-PaymentAddress)
(declare cis->CashAccount)
(declare ecis->CashAccount)
(declare new-CashAccount)
(declare cis->CashAccountChangelog)
(declare ecis->CashAccountChangelog)
(declare new-CashAccountChangelog)

;;----------------------------------------------------------------------------------
;;----------------------------------------------------------------------------------
;; Enumerations
;;----------------------------------------------------------------------------------
;;----------------------------------------------------------------------------------

;-----------------------------------------------------------------------------
; CashAccount-CashAccountStatus
;-----------------------------------------------------------------------------
(def CashAccount-CashAccountStatus-default :cash-account-status-unknown)

(def CashAccount-CashAccountStatus-val2label
  {0 :cash-account-status-unknown 1 :opening 2 :opened 3 :closing 4 :closed})

(def CashAccount-CashAccountStatus-label2val
  (set/map-invert CashAccount-CashAccountStatus-val2label))

(defn cis->CashAccount-CashAccountStatus
  [is]
  (let [val (serdes.core/cis->Enum is)]
    (get CashAccount-CashAccountStatus-val2label val val)))

(defn- get-CashAccount-CashAccountStatus
  [value]
  {:pre [(or (int? value)
             (contains? CashAccount-CashAccountStatus-label2val value))]}
  (get CashAccount-CashAccountStatus-label2val value value))

(defn write-CashAccount-CashAccountStatus
  ([tag value os]
   (write-CashAccount-CashAccountStatus tag {:optimize false} value os))
  ([tag options value os]
   (serdes.core/write-Enum tag
                           options
                           (get-CashAccount-CashAccountStatus value)
                           os)))


;;----------------------------------------------------------------------------------
;;----------------------------------------------------------------------------------
;; PaymentAddress-identifier's oneof Implementations
;;----------------------------------------------------------------------------------
;;----------------------------------------------------------------------------------

(defn convert-PaymentAddress-identifier
  [origkeyval]
  (cond
   (get-in origkeyval [:identifier :scan])
   (update-in origkeyval [:identifier :scan] new-ScanAddress)
   (get-in origkeyval [:identifier :value])
   origkeyval
   :default
   origkeyval))

(defn write-PaymentAddress-identifier
  [identifier os]
  (let [field (first identifier)
        k (when-not (nil? field) (key field))
        v (when-not (nil? field) (val field))]
    (case k
      :scan (serdes.core/write-embedded 2 v os)
      :value (serdes.core/write-String 3 {:optimize false} v os)
      nil)))



;;----------------------------------------------------------------------------------
;;----------------------------------------------------------------------------------
;; Message Implementations
;;----------------------------------------------------------------------------------
;;----------------------------------------------------------------------------------

;-----------------------------------------------------------------------------
; ScanAddress
;-----------------------------------------------------------------------------
(defrecord ScanAddress-record [sort-code account-number]
  pb/Writer
    (serialize [this os]
      (serdes.core/write-String 1 {:optimize true} (:sort-code this) os)
      (serdes.core/write-String 2 {:optimize true} (:account-number this) os))
  pb/TypeReflection
    (gettype [this] "com.repldriven.mono.schemas.cash_accounts.ScanAddress"))

(s/def :com.repldriven.mono.schemas.cash_accounts.ScanAddress/sort-code string?)
(s/def :com.repldriven.mono.schemas.cash_accounts.ScanAddress/account-number
  string?)
(s/def ::ScanAddress-spec
  (s/keys
   :opt-un
   [:com.repldriven.mono.schemas.cash_accounts.ScanAddress/sort-code
    :com.repldriven.mono.schemas.cash_accounts.ScanAddress/account-number]))
(def ScanAddress-defaults {:sort-code "" :account-number ""})

(defn cis->ScanAddress
  "CodedInputStream to ScanAddress"
  [is]
  (->> (tag-map ScanAddress-defaults
                (fn [tag index]
                  (case index
                    1 [:sort-code (serdes.core/cis->String is)]
                    2 [:account-number (serdes.core/cis->String is)]

                    [index (serdes.core/cis->undefined tag is)]))
                is)
       (map->ScanAddress-record)))

(defn ecis->ScanAddress
  "Embedded CodedInputStream to ScanAddress"
  [is]
  (serdes.core/cis->embedded cis->ScanAddress is))

(defn new-ScanAddress
  "Creates a new instance from a map, similar to map->ScanAddress except that
  it properly accounts for nested messages, when applicable.
  "
  [init]
  {:pre [(if (s/valid? ::ScanAddress-spec init)
           true
           (throw (ex-info "Invalid input"
                           (s/explain-data ::ScanAddress-spec init))))]}
  (-> (merge ScanAddress-defaults init)
      (map->ScanAddress-record)))

(defn pb->ScanAddress
  "Protobuf to ScanAddress"
  [input]
  (cis->ScanAddress (serdes.stream/new-cis input)))

(def ^:protojure.protobuf.any/record ScanAddress-meta
  {:type "com.repldriven.mono.schemas.cash_accounts.ScanAddress"
   :decoder pb->ScanAddress})

;-----------------------------------------------------------------------------
; PaymentAddress
;-----------------------------------------------------------------------------
(defrecord PaymentAddress-record [scheme identifier]
  pb/Writer
    (serialize [this os]
      (serdes.core/write-String 1 {:optimize true} (:scheme this) os)
      (write-PaymentAddress-identifier (:identifier this) os))
  pb/TypeReflection
    (gettype [this] "com.repldriven.mono.schemas.cash_accounts.PaymentAddress"))

(s/def :com.repldriven.mono.schemas.cash_accounts.PaymentAddress/scheme string?)

(s/def ::PaymentAddress-spec
  (s/keys :opt-un
          [:com.repldriven.mono.schemas.cash_accounts.PaymentAddress/scheme]))
(def PaymentAddress-defaults {:scheme ""})

(defn cis->PaymentAddress
  "CodedInputStream to PaymentAddress"
  [is]
  (->> (tag-map PaymentAddress-defaults
                (fn [tag index]
                  (case index
                    1 [:scheme (serdes.core/cis->String is)]
                    2 [:identifier {:scan (ecis->ScanAddress is)}]
                    3 [:identifier {:value (serdes.core/cis->String is)}]

                    [index (serdes.core/cis->undefined tag is)]))
                is)
       (map->PaymentAddress-record)))

(defn ecis->PaymentAddress
  "Embedded CodedInputStream to PaymentAddress"
  [is]
  (serdes.core/cis->embedded cis->PaymentAddress is))

(defn new-PaymentAddress
  "Creates a new instance from a map, similar to map->PaymentAddress except that
  it properly accounts for nested messages, when applicable.
  "
  [init]
  {:pre [(if (s/valid? ::PaymentAddress-spec init)
           true
           (throw (ex-info "Invalid input"
                           (s/explain-data ::PaymentAddress-spec init))))]}
  (-> (merge PaymentAddress-defaults init)
      (convert-PaymentAddress-identifier)
      (map->PaymentAddress-record)))

(defn pb->PaymentAddress
  "Protobuf to PaymentAddress"
  [input]
  (cis->PaymentAddress (serdes.stream/new-cis input)))

(def ^:protojure.protobuf.any/record PaymentAddress-meta
  {:type "com.repldriven.mono.schemas.cash_accounts.PaymentAddress"
   :decoder pb->PaymentAddress})

;-----------------------------------------------------------------------------
; CashAccount
;-----------------------------------------------------------------------------
(defrecord CashAccount-record [product-id party-id organization-id account-id
                               currency updated-at name account-status
                               created-at payment-addresses version-id]
  pb/Writer
    (serialize [this os]
      (serdes.core/write-String 11 {:optimize true} (:product-id this) os)
      (serdes.core/write-String 2 {:optimize true} (:party-id this) os)
      (serdes.core/write-String 8 {:optimize true} (:organization-id this) os)
      (serdes.core/write-String 1 {:optimize true} (:account-id this) os)
      (serdes.core/write-String 4 {:optimize true} (:currency this) os)
      (serdes.core/write-Int64 7 {:optimize true} (:updated-at this) os)
      (serdes.core/write-String 3 {:optimize true} (:name this) os)
      (write-CashAccount-CashAccountStatus 5
                                           {:optimize true}
                                           (:account-status this)
                                           os)
      (serdes.core/write-Int64 6 {:optimize true} (:created-at this) os)
      (serdes.complex/write-repeated serdes.core/write-embedded
                                     10
                                     (:payment-addresses this)
                                     os)
      (serdes.core/write-String 12 {:optimize true} (:version-id this) os))
  pb/TypeReflection
    (gettype [this] "com.repldriven.mono.schemas.cash_accounts.CashAccount"))

(s/def :com.repldriven.mono.schemas.cash_accounts.CashAccount/product-id
  string?)
(s/def :com.repldriven.mono.schemas.cash_accounts.CashAccount/party-id string?)
(s/def :com.repldriven.mono.schemas.cash_accounts.CashAccount/organization-id
  string?)
(s/def :com.repldriven.mono.schemas.cash_accounts.CashAccount/account-id
  string?)
(s/def :com.repldriven.mono.schemas.cash_accounts.CashAccount/currency string?)
(s/def :com.repldriven.mono.schemas.cash_accounts.CashAccount/updated-at int?)
(s/def :com.repldriven.mono.schemas.cash_accounts.CashAccount/name string?)
(s/def :com.repldriven.mono.schemas.cash_accounts.CashAccount/account-status
  (s/or :keyword keyword?
        :int int?))
(s/def :com.repldriven.mono.schemas.cash_accounts.CashAccount/created-at int?)

(s/def :com.repldriven.mono.schemas.cash_accounts.CashAccount/version-id
  string?)
(s/def ::CashAccount-spec
  (s/keys
   :opt-un
   [:com.repldriven.mono.schemas.cash_accounts.CashAccount/product-id
    :com.repldriven.mono.schemas.cash_accounts.CashAccount/party-id
    :com.repldriven.mono.schemas.cash_accounts.CashAccount/organization-id
    :com.repldriven.mono.schemas.cash_accounts.CashAccount/account-id
    :com.repldriven.mono.schemas.cash_accounts.CashAccount/currency
    :com.repldriven.mono.schemas.cash_accounts.CashAccount/updated-at
    :com.repldriven.mono.schemas.cash_accounts.CashAccount/name
    :com.repldriven.mono.schemas.cash_accounts.CashAccount/account-status
    :com.repldriven.mono.schemas.cash_accounts.CashAccount/created-at
    :com.repldriven.mono.schemas.cash_accounts.CashAccount/version-id]))
(def CashAccount-defaults
  {:product-id ""
   :party-id ""
   :organization-id ""
   :account-id ""
   :currency ""
   :updated-at 0
   :name ""
   :account-status CashAccount-CashAccountStatus-default
   :created-at 0
   :payment-addresses []
   :version-id ""})

(defn cis->CashAccount
  "CodedInputStream to CashAccount"
  [is]
  (->> (tag-map CashAccount-defaults
                (fn [tag index]
                  (case index
                    11 [:product-id (serdes.core/cis->String is)]
                    2 [:party-id (serdes.core/cis->String is)]
                    8 [:organization-id (serdes.core/cis->String is)]
                    1 [:account-id (serdes.core/cis->String is)]
                    4 [:currency (serdes.core/cis->String is)]
                    7 [:updated-at (serdes.core/cis->Int64 is)]
                    3 [:name (serdes.core/cis->String is)]
                    5 [:account-status (cis->CashAccount-CashAccountStatus is)]
                    6 [:created-at (serdes.core/cis->Int64 is)]
                    10 [:payment-addresses
                        (serdes.complex/cis->repeated ecis->PaymentAddress is)]
                    12 [:version-id (serdes.core/cis->String is)]

                    [index (serdes.core/cis->undefined tag is)]))
                is)
       (map->CashAccount-record)))

(defn ecis->CashAccount
  "Embedded CodedInputStream to CashAccount"
  [is]
  (serdes.core/cis->embedded cis->CashAccount is))

(defn new-CashAccount
  "Creates a new instance from a map, similar to map->CashAccount except that
  it properly accounts for nested messages, when applicable.
  "
  [init]
  {:pre [(if (s/valid? ::CashAccount-spec init)
           true
           (throw (ex-info "Invalid input"
                           (s/explain-data ::CashAccount-spec init))))]}
  (-> (merge CashAccount-defaults init)
      (cond-> (some? (get init :payment-addresses))
              (update :payment-addresses #(map new-PaymentAddress %)))
      (map->CashAccount-record)))

(defn pb->CashAccount
  "Protobuf to CashAccount"
  [input]
  (cis->CashAccount (serdes.stream/new-cis input)))

(def ^:protojure.protobuf.any/record CashAccount-meta
  {:type "com.repldriven.mono.schemas.cash_accounts.CashAccount"
   :decoder pb->CashAccount})

;-----------------------------------------------------------------------------
; CashAccountChangelog
;-----------------------------------------------------------------------------
(defrecord CashAccountChangelog-record [organization-id account-id status-before
                                        status-after]
  pb/Writer
    (serialize [this os]
      (serdes.core/write-String 4 {:optimize true} (:organization-id this) os)
      (serdes.core/write-String 1 {:optimize true} (:account-id this) os)
      (write-CashAccount-CashAccountStatus 2
                                           {:optimize true}
                                           (:status-before this)
                                           os)
      (write-CashAccount-CashAccountStatus 3
                                           {:optimize true}
                                           (:status-after this)
                                           os))
  pb/TypeReflection
    (gettype [this]
      "com.repldriven.mono.schemas.cash_accounts.CashAccountChangelog"))

(s/def
  :com.repldriven.mono.schemas.cash_accounts.CashAccountChangelog/organization-id
  string?)
(s/def
  :com.repldriven.mono.schemas.cash_accounts.CashAccountChangelog/account-id
  string?)
(s/def
  :com.repldriven.mono.schemas.cash_accounts.CashAccountChangelog/status-before
  (s/or :keyword keyword?
        :int int?))
(s/def
  :com.repldriven.mono.schemas.cash_accounts.CashAccountChangelog/status-after
  (s/or :keyword keyword?
        :int int?))
(s/def ::CashAccountChangelog-spec
  (s/keys
   :opt-un
   [:com.repldriven.mono.schemas.cash_accounts.CashAccountChangelog/organization-id
    :com.repldriven.mono.schemas.cash_accounts.CashAccountChangelog/account-id
    :com.repldriven.mono.schemas.cash_accounts.CashAccountChangelog/status-before
    :com.repldriven.mono.schemas.cash_accounts.CashAccountChangelog/status-after]))
(def CashAccountChangelog-defaults
  {:organization-id ""
   :account-id ""
   :status-before CashAccount-CashAccountStatus-default
   :status-after CashAccount-CashAccountStatus-default})

(defn cis->CashAccountChangelog
  "CodedInputStream to CashAccountChangelog"
  [is]
  (->> (tag-map CashAccountChangelog-defaults
                (fn [tag index]
                  (case index
                    4 [:organization-id (serdes.core/cis->String is)]
                    1 [:account-id (serdes.core/cis->String is)]
                    2 [:status-before (cis->CashAccount-CashAccountStatus is)]
                    3 [:status-after (cis->CashAccount-CashAccountStatus is)]

                    [index (serdes.core/cis->undefined tag is)]))
                is)
       (map->CashAccountChangelog-record)))

(defn ecis->CashAccountChangelog
  "Embedded CodedInputStream to CashAccountChangelog"
  [is]
  (serdes.core/cis->embedded cis->CashAccountChangelog is))

(defn new-CashAccountChangelog
  "Creates a new instance from a map, similar to map->CashAccountChangelog except that
  it properly accounts for nested messages, when applicable.
  "
  [init]
  {:pre [(if (s/valid? ::CashAccountChangelog-spec init)
           true
           (throw (ex-info "Invalid input"
                           (s/explain-data ::CashAccountChangelog-spec
                                           init))))]}
  (-> (merge CashAccountChangelog-defaults init)
      (map->CashAccountChangelog-record)))

(defn pb->CashAccountChangelog
  "Protobuf to CashAccountChangelog"
  [input]
  (cis->CashAccountChangelog (serdes.stream/new-cis input)))

(def ^:protojure.protobuf.any/record CashAccountChangelog-meta
  {:type "com.repldriven.mono.schemas.cash_accounts.CashAccountChangelog"
   :decoder pb->CashAccountChangelog})

