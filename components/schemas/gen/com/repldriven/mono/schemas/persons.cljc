;;;----------------------------------------------------------------------------------
;;; Generated by protoc-gen-clojure.  DO NOT EDIT
;;;
;;; Message Implementation of package com.repldriven.mono.schemas.persons
;;;----------------------------------------------------------------------------------
(ns com.repldriven.mono.schemas.persons
  (:require
    [protojure.protobuf.protocol :as pb]
    [protojure.protobuf.serdes.core :as serdes.core]
    [protojure.protobuf.serdes.complex :as serdes.complex]
    [protojure.protobuf.serdes.utils :refer [tag-map]]
    [protojure.protobuf.serdes.stream :as serdes.stream]
    [clojure.set :as set]
    [clojure.spec.alpha :as s]))

;;----------------------------------------------------------------------------------
;;----------------------------------------------------------------------------------
;; Forward declarations
;;----------------------------------------------------------------------------------
;;----------------------------------------------------------------------------------

(declare cis->Person)
(declare ecis->Person)
(declare new-Person)
(declare cis->Person-PhoneNumber)
(declare ecis->Person-PhoneNumber)
(declare new-Person-PhoneNumber)

;;----------------------------------------------------------------------------------
;;----------------------------------------------------------------------------------
;; Enumerations
;;----------------------------------------------------------------------------------
;;----------------------------------------------------------------------------------

;-----------------------------------------------------------------------------
; Person-PhoneType
;-----------------------------------------------------------------------------
(def Person-PhoneType-default :mobile)

(def Person-PhoneType-val2label {0 :mobile 1 :home 2 :work})

(def Person-PhoneType-label2val (set/map-invert Person-PhoneType-val2label))

(defn cis->Person-PhoneType
  [is]
  (let [val (serdes.core/cis->Enum is)]
    (get Person-PhoneType-val2label val val)))

(defn- get-Person-PhoneType
  [value]
  {:pre [(or (int? value) (contains? Person-PhoneType-label2val value))]}
  (get Person-PhoneType-label2val value value))

(defn write-Person-PhoneType
  ([tag value os] (write-Person-PhoneType tag {:optimize false} value os))
  ([tag options value os]
   (serdes.core/write-Enum tag options (get-Person-PhoneType value) os)))



;;----------------------------------------------------------------------------------
;;----------------------------------------------------------------------------------
;; Message Implementations
;;----------------------------------------------------------------------------------
;;----------------------------------------------------------------------------------

;-----------------------------------------------------------------------------
; Person
;-----------------------------------------------------------------------------
(defrecord Person-record [id name email phones]
  pb/Writer
    (serialize [this os]
      (serdes.core/write-Int32 1 {:optimize true} (:id this) os)
      (serdes.core/write-String 2 {:optimize true} (:name this) os)
      (serdes.core/write-String 3 {:optimize true} (:email this) os)
      (serdes.complex/write-repeated serdes.core/write-embedded
                                     4
                                     (:phones this)
                                     os))
  pb/TypeReflection
    (gettype [this] "com.repldriven.mono.schemas.persons.Person"))

(s/def :com.repldriven.mono.schemas.persons.Person/id int?)
(s/def :com.repldriven.mono.schemas.persons.Person/name string?)
(s/def :com.repldriven.mono.schemas.persons.Person/email string?)

(s/def ::Person-spec
  (s/keys :opt-un [:com.repldriven.mono.schemas.persons.Person/id
                   :com.repldriven.mono.schemas.persons.Person/name
                   :com.repldriven.mono.schemas.persons.Person/email]))
(def Person-defaults {:id 0 :name "" :email "" :phones []})

(defn cis->Person
  "CodedInputStream to Person"
  [is]
  (->> (tag-map Person-defaults
                (fn [tag index]
                  (case index
                    1 [:id (serdes.core/cis->Int32 is)]
                    2 [:name (serdes.core/cis->String is)]
                    3 [:email (serdes.core/cis->String is)]
                    4 [:phones
                       (serdes.complex/cis->repeated ecis->Person-PhoneNumber
                                                     is)]

                    [index (serdes.core/cis->undefined tag is)]))
                is)
       (map->Person-record)))

(defn ecis->Person
  "Embedded CodedInputStream to Person"
  [is]
  (serdes.core/cis->embedded cis->Person is))

(defn new-Person
  "Creates a new instance from a map, similar to map->Person except that
  it properly accounts for nested messages, when applicable.
  "
  [init]
  {:pre [(if (s/valid? ::Person-spec init)
           true
           (throw (ex-info "Invalid input"
                           (s/explain-data ::Person-spec init))))]}
  (-> (merge Person-defaults init)
      (cond-> (some? (get init :phones))
              (update :phones #(map new-Person-PhoneNumber %)))
      (map->Person-record)))

(defn pb->Person
  "Protobuf to Person"
  [input]
  (cis->Person (serdes.stream/new-cis input)))

(def ^:protojure.protobuf.any/record Person-meta
  {:type "com.repldriven.mono.schemas.persons.Person" :decoder pb->Person})

;-----------------------------------------------------------------------------
; Person-PhoneNumber
;-----------------------------------------------------------------------------
(defrecord Person-PhoneNumber-record [number type]
  pb/Writer
    (serialize [this os]
      (serdes.core/write-String 1 {:optimize true} (:number this) os)
      (write-Person-PhoneType 2 {:optimize true} (:type this) os))
  pb/TypeReflection
    (gettype [this] "com.repldriven.mono.schemas.persons.Person-PhoneNumber"))

(s/def :com.repldriven.mono.schemas.persons.Person-PhoneNumber/number string?)
(s/def :com.repldriven.mono.schemas.persons.Person-PhoneNumber/type
  (s/or :keyword keyword?
        :int int?))
(s/def ::Person-PhoneNumber-spec
  (s/keys :opt-un
          [:com.repldriven.mono.schemas.persons.Person-PhoneNumber/number
           :com.repldriven.mono.schemas.persons.Person-PhoneNumber/type]))
(def Person-PhoneNumber-defaults {:number "" :type Person-PhoneType-default})

(defn cis->Person-PhoneNumber
  "CodedInputStream to Person-PhoneNumber"
  [is]
  (->> (tag-map Person-PhoneNumber-defaults
                (fn [tag index]
                  (case index
                    1 [:number (serdes.core/cis->String is)]
                    2 [:type (cis->Person-PhoneType is)]

                    [index (serdes.core/cis->undefined tag is)]))
                is)
       (map->Person-PhoneNumber-record)))

(defn ecis->Person-PhoneNumber
  "Embedded CodedInputStream to Person-PhoneNumber"
  [is]
  (serdes.core/cis->embedded cis->Person-PhoneNumber is))

(defn new-Person-PhoneNumber
  "Creates a new instance from a map, similar to map->Person-PhoneNumber except that
  it properly accounts for nested messages, when applicable.
  "
  [init]
  {:pre [(if (s/valid? ::Person-PhoneNumber-spec init)
           true
           (throw (ex-info "Invalid input"
                           (s/explain-data ::Person-PhoneNumber-spec init))))]}
  (-> (merge Person-PhoneNumber-defaults init)
      (map->Person-PhoneNumber-record)))

(defn pb->Person-PhoneNumber
  "Protobuf to Person-PhoneNumber"
  [input]
  (cis->Person-PhoneNumber (serdes.stream/new-cis input)))

(def ^:protojure.protobuf.any/record Person-PhoneNumber-meta
  {:type "com.repldriven.mono.schemas.persons.Person-PhoneNumber"
   :decoder pb->Person-PhoneNumber})

