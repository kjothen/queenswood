(ns com.repldriven.mono.bank-idv-onfido-webhook.interface
  (:require
    [com.repldriven.mono.bank-idv-onfido-webhook.components
     :as components]))

(def component-registry components/component-registry)

(def example-registry components/example-registry)
