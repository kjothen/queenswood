- kind:
    transaction: {}
  bound:
    kind:
      max:
        aggregate:
          kind:
            count:
              value: 1000
              window: !keyword time-window-daily
  reason: "Micro tier - max 1,000 transactions per day"
