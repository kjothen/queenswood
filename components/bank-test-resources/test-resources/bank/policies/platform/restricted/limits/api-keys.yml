- kind:
    api-key: {}
  bound:
    kind:
      max:
        aggregate:
          kind:
            count:
              value: 1000
              window: !keyword time-window-instant
  reason: "Platform-restricted - max 1,000 active API keys"
