- kind:
    payee-check: {}
  bound:
    kind:
      max:
        aggregate:
          kind:
            count:
              value: 1000
              window: !keyword time-window-daily
  reason: "Platform-restricted - max 1,000 payee checks per day"
