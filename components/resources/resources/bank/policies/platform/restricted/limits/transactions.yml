- kind:
    transaction: {}
  bound:
    kind:
      max:
        aggregate:
          kind:
            count:
              value: 1000000
              window: !keyword time-window-daily
  reason: "Platform-restricted - max 1,000,000 transactions per day"
