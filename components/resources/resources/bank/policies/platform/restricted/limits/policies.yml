- kind:
    policy: {}
  bound:
    kind:
      max:
        aggregate:
          kind:
            count:
              value: 100
              window: !keyword time-window-instant
  reason: "Platform-restricted - max 100 policies"
