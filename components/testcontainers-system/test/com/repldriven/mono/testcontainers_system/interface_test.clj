(ns com.repldriven.mono.testcontainers-system.interface-test
  (:require [clojure.test :as test :refer :all]
            [com.repldriven.mono.testcontainers-system.interface :as testcontainers-system]))

(deftest dummy-test
  (is (= 1 1)))
