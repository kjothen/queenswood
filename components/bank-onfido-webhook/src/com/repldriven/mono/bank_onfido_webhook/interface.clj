(ns com.repldriven.mono.bank-onfido-webhook.interface
  (:require
    [com.repldriven.mono.bank-onfido-webhook.components
     :as components]))

(def component-registry components/component-registry)

(def example-registry components/example-registry)
